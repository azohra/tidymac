interface:
  display_name: "TidyMac"
  short_description: "Deeply investigate and visualize Mac storage"
  default_prompt: "Use $tidymac to deeply investigate my Mac, reconcile what is using space, and present a safe cleanup plan with an interactive opportunity map when supported."
